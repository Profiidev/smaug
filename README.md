# Smaug

# require always one user in admin group

# user edit site (password reset (if no mail active), group management, avatar reset, change name)

# group management and password reset only for user with less or equal permissions

# user list show avatar

# nav menu sorting and grouping
